version: '3.8'

services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/app/notebooks
      - ./outputs:/app/outputs
      - ./analytics.duckdb:/app/analytics.duckdb
    container_name: phase3-ds
